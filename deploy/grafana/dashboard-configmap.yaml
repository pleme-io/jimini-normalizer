apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-jimini-normalizer
  namespace: observability
  labels:
    grafana_dashboard: "1"
    app.kubernetes.io/part-of: nexus-platform
    component: jimini-normalizer
data:
  jimini-normalizer.json: |
    {{- .Files.Get "dashboard.json" | nindent 4 }}
